# Settlr API

Go/PostgreSQL API snapshot for Settlr. The source is copied from the running workspace for version control; production credentials, uploads, database volumes, and live configuration are intentionally excluded.
